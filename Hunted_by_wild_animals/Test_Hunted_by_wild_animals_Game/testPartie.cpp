#ifndef TESTPARTIE_H
#define TESTPARTIE_H

#include "doctest.h"
#include "../Hunted_by_wild_animals_Game/partie.h"
#include "../Hunted_by_wild_animals_Game/terrain.h"


TEST_CASE("Test de la classe partie")
{/*
    std::unique_ptr<interface> interface = std::make_unique<interfaceT>();
    int hauteur =5, largeur = 6;
    terrain t{hauteur,largeur};
    partie p{t,std::move(interface)};

    SUBCASE("Test de la fonction position valide")
    {
       position pos{10,10};
       bool valide = p.positionValide(pos);
       REQUIRE_EQ(0,0);

    }
*/
}

#endif

